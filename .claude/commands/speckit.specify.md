---
description: Create or update the feature specification from a natural language feature description.
argument-hint: [input]
---

Execute the Spec Kit `speckit.specify` command for this project.

Read the prompt file `.speckit/commands/speckit.specify.md` and follow its instructions **exactly**, treating this invocation as running that command. The generic Spec Kit prompts under `.speckit/commands/` are the single source of truth; do not duplicate their logic here.

User input / arguments to pass through as `$ARGUMENTS`:

$ARGUMENTS
