---
description: Assess the current codebase against the feature's spec, plan, and tasks, then append any remaining unbuilt work as new tasks to tasks.md so implement can complete it.
argument-hint: [input]
---

Execute the Spec Kit `speckit.converge` command for this project.

Read the prompt file `.speckit/commands/speckit.converge.md` and follow its instructions **exactly**, treating this invocation as running that command. The generic Spec Kit prompts under `.speckit/commands/` are the single source of truth; do not duplicate their logic here.

User input / arguments to pass through as `$ARGUMENTS`:

$ARGUMENTS
