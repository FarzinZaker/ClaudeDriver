---
description: Perform a non-destructive cross-artifact consistency and quality analysis across spec.md, plan.md, and tasks.md after task generation.
argument-hint: [input]
---

Execute the Spec Kit `speckit.analyze` command for this project.

Read the prompt file `.speckit/commands/speckit.analyze.md` and follow its instructions **exactly**, treating this invocation as running that command. The generic Spec Kit prompts under `.speckit/commands/` are the single source of truth; do not duplicate their logic here.

User input / arguments to pass through as `$ARGUMENTS`:

$ARGUMENTS
