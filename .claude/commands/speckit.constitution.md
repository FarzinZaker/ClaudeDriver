---
description: Create or update the project constitution from interactive or provided principle inputs.
argument-hint: [input]
---

Execute the Spec Kit `speckit.constitution` command for this project.

Read the prompt file `.speckit/commands/speckit.constitution.md` and follow its instructions **exactly**, treating this invocation as running that command. The generic Spec Kit prompts under `.speckit/commands/` are the single source of truth; do not duplicate their logic here.

User input / arguments to pass through as `$ARGUMENTS`:

$ARGUMENTS
