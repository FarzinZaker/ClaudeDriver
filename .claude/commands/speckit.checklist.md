---
description: Generate a custom checklist for the current feature based on user requirements.
argument-hint: [input]
---

Execute the Spec Kit `speckit.checklist` command for this project.

Read the prompt file `.speckit/commands/speckit.checklist.md` and follow its instructions **exactly**, treating this invocation as running that command. The generic Spec Kit prompts under `.speckit/commands/` are the single source of truth; do not duplicate their logic here.

User input / arguments to pass through as `$ARGUMENTS`:

$ARGUMENTS
