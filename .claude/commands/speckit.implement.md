---
description: Execute the implementation plan by processing and executing all tasks defined in tasks.md
argument-hint: [input]
---

Execute the Spec Kit `speckit.implement` command for this project.

Read the prompt file `.speckit/commands/speckit.implement.md` and follow its instructions **exactly**, treating this invocation as running that command. The generic Spec Kit prompts under `.speckit/commands/` are the single source of truth; do not duplicate their logic here.

User input / arguments to pass through as `$ARGUMENTS`:

$ARGUMENTS
